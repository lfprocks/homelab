apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pnp

resources:
  - writer.yaml
  - writer_secret.yaml
  - deliverer.yaml
  - deliverer_secret.yaml
labels:
  - includeSelectors: true
    pairs:
      proj: pulsar-notification-pipeline
