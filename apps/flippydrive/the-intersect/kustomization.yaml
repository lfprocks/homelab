apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flippydrive

resources:
  - deployment.yaml
  - service.yaml
  - persistentvolumeclaim.yaml

labels:
  - includeSelectors: true
    pairs:
      env: prod
      proj: flippydrive