apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homebox

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - httproute.yaml
  - secret.sops.yaml

labels:
  - includeSelectors: true
    pairs:
      env: prod
      proj: homebox
