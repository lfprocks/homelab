apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: openclaw-warmpool
spec:
  # One pre-warmed sandbox so the claim resolves immediately.
  replicas: 1
  sandboxTemplateRef:
    name: openclaw-template
