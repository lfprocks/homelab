apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: immich
resources:
  # - dragonfly.yaml
  - postgres.yaml
  - httproute.yaml
  - persistentvolumeclaim.yaml
  - helmrelease.yaml
  - external-library-pvc.yaml
  - values-secret.sops.yaml
configMapGenerator:
  - name: immich-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
