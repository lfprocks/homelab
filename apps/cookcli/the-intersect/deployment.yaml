---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cookcli-env-configmap
data:
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: &name cookcli
  labels:
    app: *name
spec:
  selector:
    matchLabels:
      app: *name
  revisionHistoryLimit: 10
  replicas: 1
  template:
    metadata:
      labels:
        app: *name
        k8s.lfp.rocks/service: "cookcli"
        k8s.lfp.rocks/version: "v0.30.0"
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: k8s.lfp.rocks/gpu
                    operator: NotIn
                    values:
                      - enabled
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: *name
          # Image default ENTRYPOINT/CMD already runs `cook server /recipes --host`
          # binding 0.0.0.0:9080, so no command/args override is needed.
          image: ghcr.io/cooklang/cookcli:0.30.0
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: cookcli-env-configmap
          ports:
            - name: http
              containerPort: 9080
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 5
          resources:
            limits:
              memory: "256Mi"
            requests:
              cpu: "20m"
              memory: "64Mi"
          volumeMounts:
            - name: recipes
              mountPath: /recipes
              readOnly: true
      volumes:
        - name: recipes
          configMap:
            name: cookcli-recipes
