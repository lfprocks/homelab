# NOTE: this file ships unencrypted as a TEMPLATE. Put the allowed emails (one
# per line, matching the email claim Pocket-ID returns) in
# authenticated-emails.txt, then encrypt before committing:
#   sops -e --in-place apps/cookcli/the-intersect/oauth2-proxy-emails-secret.yaml
# This is defense-in-depth on top of needing a Pocket-ID account/passkey.
apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-emails
type: Opaque
stringData:
  authenticated-emails.txt: |
    michael@michaelpeterswa.com
