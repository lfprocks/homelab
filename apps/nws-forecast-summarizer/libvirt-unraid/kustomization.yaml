apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nws

resources:
  - ../base/
labels:
  - includeSelectors: true
    pairs:
      env: prod
      proj: nws-forecast-summarizer
patches:
  - path: api.yaml
  - path: api_hpa.yaml
